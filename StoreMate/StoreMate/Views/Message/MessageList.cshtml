@model List<Message>
@{
    ViewData["Title"] = "MessageList";
    Layout = "~/Views/Layout/Index.cshtml";
    int count = 0;
}
<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Mesaj Listesi</h4>
                <p class="card-description">

                </p>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Gönderen</th>
                                <th>Konu</th>
                                <th>Durum</th>
                                <th>Tarih</th>
                                <th>Sil</th>
                                <th>Güncelle</th>
                                <th>Mesajı Aç</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                count  ++;
                                <tr>
                                    <th>@count</th>
                                    <td>@item.SenderNameSurname</td>
                                    <td>@item.Title</td>
                                    <td>@item.IsRead</td>
                                    <td>@item.Datetime</td>
                                     
                                    
                                    <td><a href="/Message/DeleteMessage/@item.MessageId" class="btn btn-danger">Sil</a></td>
                                    <td><a href="/Message/UpdateMessage/@item.MessageId" class="btn btn-success">Güncelle</a></td>
                                    <td><a href="/Message/UpdateMessage/@item.MessageId" class="btn btn-warning">Mesajı Görüntüle</a></td>
                                </tr>
                            }

                        </tbody>
                    </table>
                    <a href="/Message/CreateMessage/" class="btn btn-primary">Yeni Mesaj Ekle</a>
                </div>
            </div>
        </div>
    </div>
</div>
